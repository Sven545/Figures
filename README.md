# Figures
1) «адание с библиотекой

ƒобавление новых фигур производитс€ с помощью наследовани€ от 
абстрактного класса Figure, абстрактный метод GetArea() которого 
позвол€ет вычисл€ть площадь любой фигуры, унаследованной от данного класса.
“аким образом выполн€етс€ условие "¬ычисление площади фигуры без знани€ типа фигуры в compile-time".

¬ классе Triangle определен метод RectangularityCheck(), определ€ющий, €вл€етс€ ли треугольник пр€моугольным.
“акже в классах фигур определен метод ExistenceCheck(), который в конструкторе провер€ет, 
может ли существовать фигура с заданными параметрами. » если существование фигуры невозможно,
генерируетс€ ArgumentException, поэтому вызывающий код должен обрабатывать данное исключение,
которое может произойти на этапе создани€ новой фигуры.

“акже были добавлены Unit тесты, провер€ющие как различные сценарии создани€ обЏектов фигур,
так и функции вычислени€ площадей фигур и проверку пр€моугольности треугольника.


2) «адание с SQL запросом

SELECT Products.ProductName, Categories.CategoryName
FROM Products
LEFT JOIN Categories ON Products.ProductId=Categories.ProductId